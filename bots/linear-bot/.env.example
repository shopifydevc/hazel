# Linear Bot Configuration

# Required: Bot authentication token from Hazel
BOT_TOKEN=your-bot-token-here

# Required: Database connection for fetching integration tokens
DATABASE_URL=postgresql://user:password@localhost:5432/hazel

# Required: Encryption key for decrypting integration tokens (base64 encoded 32-byte key)
INTEGRATION_ENCRYPTION_KEY=your-base64-encoded-32-byte-key

# Optional: Encryption key version (defaults to 1)
# INTEGRATION_ENCRYPTION_KEY_VERSION=1

# Optional: Set to false for production (enables SSL on DB connection)
# IS_DEV=true

# Optional: Override URLs for local development
# ELECTRIC_URL=http://localhost:8787/v1/shape
# BACKEND_URL=http://localhost:3003
# REDIS_URL=redis://localhost:6379
